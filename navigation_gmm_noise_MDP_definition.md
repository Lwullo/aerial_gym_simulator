# Navigation GMM Noise Task - MDP / 观测定义（当前代码版本）

> 任务名称：`navigation_task_gmm_noise`
>
> 文档版本：v3.1
>
> 最后更新：2026-05-17

---

## 1. 文档范围

本文档与当前实现保持同步，基于以下代码：

- `aerial_gym/task/navigation_task_gmm_noise/navigation_task_gmm_noise.py`
- `aerial_gym/config/task_config/navigation_task_gmm_noise_config.py`

文档的主要目的，是给出当前训练代码中关于状态、观测、动作以及输入通路的准确定义，便于你后续直接用于论文或实验报告撰写。

---

## 2. MDP 与观测设定

从仿真环境层面看，该任务仍然可以写成标准形式：

`MDP = (S, A, P, R)`

- `S`：完整状态空间
- `A`：动作空间
- `P`：状态转移动力学
- `R`：奖励函数

但需要强调的是，策略网络并不能直接观测到完整状态 `S`。因此：

- 从环境真实动力学角度，它有一个底层的 MDP 状态；
- 从 Actor 实际拿到的信息角度，这个任务更准确地说是一个 **部分可观测马尔可夫决策过程（POMDP）**。

为了缓解部分可观测性，当前代码使用了两种手段：

- Actor 侧使用 `6` 帧历史观测堆叠；
- Critic 侧使用非对称特权信息输入。

---

## 3. 完整状态 `S`（概念层面）

代码里并没有显式把完整状态拼成一个统一的大向量返回给 Actor。若从概念上理解，当前环境的完整状态至少包含：

- 母机的位置、姿态、线速度、角速度；
- 目标点位置；
- 投放相关内部状态，例如当前是否已经投放；
- 每个 episode 采样得到的风场参数；
- 当前 Dryden 湍流内部状态，或者局部阵风内部状态；
- 机器人当前质量和惯量；
- 若当前时刻投放时的预期反冲量；
- 与奖励、统计和课程学习有关的一些内部计数器或运行量。

因此，Actor 实际拿到的只是完整状态的一部分投影，而不是完整马尔可夫状态。

---

## 4. 基础观测 Base Observation

### 4.1 维度

当前基础观测维度为 **13 维**。

### 4.2 定义

每个控制步，环境首先构造一个基础观测：

`o_base in R^13`

其排列如下：

`o_base = [p_rel_body(3), v_body(3), roll, pitch, yaw, omega_body(3), z_world]`

更具体地写为：

| 下标 | 符号 | 含义 | 坐标系 |
|---|---|---|---|
| `0:3` | `p_rel_body` | 目标相对位置，即 `target_position - robot_position`，再旋转到机体系 | 机体系 |
| `3:6` | `v_body` | 无人机线速度 | 机体系 |
| `6` | `roll` | 经过 `ssa` 处理后的横滚角 | 角度量 |
| `7` | `pitch` | 经过 `ssa` 处理后的俯仰角 | 角度量 |
| `8` | `yaw` | 经过 `ssa` 处理后的偏航角 | 角度量 |
| `9:12` | `omega_body` | 无人机角速度 | 机体系 |
| `12` | `z_world` | 无人机绝对高度 | 世界系 |

### 4.3 说明

- 与旧版本文档不同，当前基础观测已经不是 `12` 维，而是 **13 维**；
- 新增的一维就是 `yaw`；
- 相对目标位置使用 `quat_rotate_inverse(...)` 转到了机体系；
- `roll/pitch/yaw` 来自 `robot_euler_angles`，并经过 `ssa(...)` 包角处理；
- 基础观测中 **不直接包含** 真实风速、质量、惯量或反冲信息。

---

## 5. Actor 输入：6 帧观测堆叠

### 5.1 帧堆叠方式

Actor 实际并不是只看单帧 `o_base`，而是看最近 `6` 帧基础观测的拼接：

`o_actor = [o_base(t-5), ..., o_base(t)]`

因此 Actor 输入维度为：

`13 x 6 = 78`

### 5.2 Reset 时的处理

环境 reset 时，代码会：

- 先计算一次初始基础观测；
- 再用这一帧把整个 6 帧缓冲区全部填满。

因此 reset 后最开始的输入满足：

`o_base(t-5) = ... = o_base(t)`

### 5.3 运行过程中的更新逻辑

每个控制步都会执行：

1. 去掉最旧的一帧；
2. 将当前新观测压入缓冲区末尾；
3. 将 6 帧展平得到一个 `78` 维向量，送给 Actor。

### 5.4 这样做的意义

这种设计使 Actor 能够从短时间窗口内隐式感知：

- 速度变化趋势；
- 姿态变化趋势；
- 单帧无法直接观测到的扰动影响。

在当前项目里，这个 6 帧堆叠逻辑已经替代了更早期的历史增强 / RNN 风格输入方案。

---

## 6. Critic 的特权观测 Privileged Observation

### 6.1 维度

当前特权观测维度为 **13 维**。

### 6.2 定义

Critic 额外使用一个特权向量：

`o_priv in R^13`

排列如下：

`o_priv = [wind_world(3), mass(1), inertia_diag(3), recoil_force_world(3), recoil_tau_world(3)]`

具体含义如下：

| 下标 | 符号 | 含义 | 坐标系 |
|---|---|---|---|
| `0:3` | `wind_world` | 当前环境实际使用的世界系风速向量 | 世界系 |
| `3:4` | `mass` | 当前机器人质量 | 标量 |
| `4:7` | `inertia_diag` | 当前惯量张量对角项 `[Jxx, Jyy, Jzz]` | 参数量 |
| `7:10` | `recoil_force_world` | 若此刻立即投放，预计产生的反冲力 | 世界系 |
| `10:13` | `recoil_tau_world` | 若此刻立即投放，预计产生的反冲力矩 | 世界系 |

### 6.3 预期反冲项的含义

这里的反冲项是一个“反事实预测量”，意思是：

- 它并不表示当前已经发生了反冲；
- 它表示“如果此刻投放，理论上会出现多大的反冲力和反冲力矩”。

这类信息只提供给 Critic，有助于训练阶段更好地评估某一时刻的投放质量。

---

## 7. Critic 输入：非对称 Actor-Critic

### 7.1 默认设置

当前代码默认采用非对称 Actor-Critic：

- Actor 输入：仅使用 `6` 帧堆叠观测；
- Critic 输入：`6` 帧堆叠观测 + 特权信息。

因此：

`o_critic = [o_actor, o_priv]`

维度分别为：

- Actor 输入：`78`
- 特权输入：`13`
- Critic 输入：`91`

### 7.2 在代码中的实际通路

在任务返回的观测字典中：

- `task_obs["observations"]` 存放 Actor 输入；
- `task_obs["states"]` 存放 Critic 输入。

这也是当前 `rl_games` 训练时真正使用的输入通道。

### 7.3 当前代码中已有的可选模式

当前实现里还保留了两个可选开关：

1. `critic_use_privileged_obs`

- 若为 `True`，Critic 输入为 `78 + 13`；
- 若为 `False`，Critic 与 Actor 一样，都只使用 `78` 维堆叠观测。

2. `critic_privileged_mode`

- `"full"`：使用完整 13 维特权信息；
- `"wind_only"`：只保留前 3 维真实风速，其余 `3:13` 清零。

也就是说，在 `"wind_only"` 模式下，Critic 仍然接收 13 维特权块，但其中只有前 3 维是有效信息。

### 7.4 特权随机化

代码中还有一个可选开关：

`privileged_randomization_enable`

若打开，该特权信息在输入 Critic 前会被随机缩放。当前已支持分别对以下量进行随机缩放：

- 风速；
- 质量；
- 惯量；
- 反冲力；
- 反冲力矩。

这个机制本质上是一种 Critic 侧的特权域随机化，不属于 Actor 的观测内容。

---

## 8. 输入归一化与数值尺度处理

### 8.1 当前项目中的归一化位置

当前项目采用的是：

- **环境侧不手工归一化**
- **训练框架侧进行在线输入归一化**

也就是说，环境在构造 `o_base`、`o_actor` 和 `o_priv` 时，保留的是原始物理量形式，例如：

- 相对位置：单位为米
- 线速度：单位为 `m/s`
- 欧拉角：单位为弧度
- 角速度：单位为 `rad/s`
- 高度：单位为米
- 特权信息中的质量、惯量、反冲力和反冲力矩也都保持各自物理量纲

因此，**环境本身并没有额外设计手工缩放规则**，例如：

- 没有统一除以某个固定最大值
- 没有把全部特征硬映射到 `[-1, 1]`
- 没有对不同维度单独写死人工归一化系数

### 8.2 实际生效的归一化机制

虽然环境侧不手工归一化，但当前训练配置中启用了 `rl_games` 的运行时输入归一化：

- Actor 输入启用 `normalize_input = True`
- Critic / central value 输入也启用 `normalize_input = True`

因此，网络真正接收到的并不是未经处理的原始量纲数据，而是：

> 原始观测经过运行时统计得到的在线标准化输入

从实现逻辑上看，可以理解为：

```text
环境输出原始观测  -->  rl_games 在线统计均值/方差  -->  归一化后送入网络
```

也就是说：

- `task_obs["observations"]` 会在进入 Actor 前做运行时归一化；
- `task_obs["states"]` 会在进入 Critic 前做运行时归一化。

### 8.3 为什么要加入归一化

当前任务的输入由多类物理量共同组成，不同维度之间存在明显量纲差异和数值范围差异。例如：

- 相对位置可能达到十几米甚至更大；
- 线速度通常在 `1 m/s` 量级；
- 欧拉角通常远小于 `1 rad`；
- 角速度和高度又处于不同数量级；
- Critic 侧还额外包含质量、惯量、反冲力和反冲力矩等物理量。

如果不进行归一化，可能出现以下问题：

1. **大尺度特征主导训练**

- 数值范围较大的特征更容易在网络前几层占据主导地位；
- 数值范围较小但对决策同样重要的特征，容易被淹没。

2. **梯度更新不稳定**

- PPO 在面对多量纲混合输入时，对学习率和网络初始化会更敏感；
- 输入尺度差异过大时，训练波动和收敛不稳定问题通常会更明显。

3. **Actor 与 Critic 输入分布更复杂**

- 你的 Actor 使用 `78` 维堆叠观测；
- Critic 使用 `91` 维非对称输入；
- 若不归一化，这类高维混合输入更容易导致优化困难。

因此，在当前项目中加入输入归一化的主要目的，是：

- 减小不同物理量纲之间的数值尺度差异；
- 提高 PPO 优化过程的稳定性；
- 减少训练对超参数的敏感性；
- 帮助网络更均衡地利用各维输入信息。

### 8.4 为什么不在环境侧手工归一化，而是采用框架在线归一化

对于当前项目，采用训练框架提供的在线归一化，而不是在环境侧手工写死归一化规则，主要有以下几个原因：

1. **避免重复归一化**

当前训练配置已经启用了 `rl_games` 的 `normalize_input`。如果再在环境侧手工额外归一化，容易形成“双重归一化”，反而增加解释和调试难度。

2. **保持物理含义清晰**

环境直接输出原始物理量，更方便从建模角度理解每一维观测的真实意义，也更利于论文中对状态变量本身进行解释。

3. **在线归一化更适应实际分布**

与固定手工比例缩放相比，运行时归一化能够随着训练过程中状态分布的变化，自适应调整输入统计量，更适合当前这类风扰、随机高度、随机初始位置共同作用的任务。

4. **便于多实验保持一致**

你当前项目中已经存在多个基线和对比实验。将归一化统一交给训练框架处理，可以避免不同环境版本因为手工缩放策略不同而影响实验公平性。

### 8.5 论文中的推荐表述

若用于论文正文，可以采用下面这段表述：

> 本文环境返回的观测保持原始物理量形式，包括相对位置、速度、姿态角、角速度、高度以及 Critic 侧的特权物理量，并未在环境层面手工设计固定比例缩放。考虑到不同观测维度具有明显不同的量纲和数值范围，例如位置、速度、姿态角、质量、惯量及反冲量处于不同尺度，为降低输入尺度差异对策略优化的不利影响，训练阶段采用 `rl_games` 提供的运行时输入归一化机制，对 Actor 与 Critic 输入进行在线标准化处理。该设计能够提高 PPO 训练稳定性，减弱不同量纲特征对梯度更新的不均衡影响，并提升多源物理量混合输入下的收敛鲁棒性。

### 8.6 本项目当前结论

因此，对你当前项目而言，可以准确总结为：

- **需要归一化**
- **已经启用了归一化**
- **归一化发生在训练框架侧，而不是环境手工缩放侧**

---

## 9. 动作空间 `A`

### 8.1 维度

动作是一个 **5 维连续向量**：

`a = [vx_cmd, vy_cmd, vz_cmd, yawrate_cmd, drop_switch]`

策略网络输出范围为 `[-1, 1]`。

### 8.2 物理映射

当前动作变换函数会将：

- `vx_cmd, vy_cmd, vz_cmd` 映射到 `[-1.2, 1.2] m/s`
- `yawrate_cmd` 映射到 `[-pi/6, pi/6] rad/s`
- `drop_switch` 保留原始归一化输出，用于阈值触发投放

### 8.3 投放触发条件

当前代码中，当满足下式时请求投放：

`drop_switch > drop_threshold`

当前默认配置为：

`drop_threshold = 0.7`

---

## 10. 环境、出生点与目标点

### 9.1 环境边界

当前任务使用固定边界：

- `x in [-25, 25]`
- `y in [-25, 25]`
- `z in [0, 15]`

### 9.2 目标点

当前默认目标点配置为：

- `target_use_fixed = True`
- `target_fixed_use_env_center = True`
- `target_fixed_position = [0, 0, 0]`

因此，在当前默认训练设置下，目标点固定在每个并行环境中心的地面位置。

### 9.3 母机出生点

当前默认母机出生策略为：

- `x ~ Uniform[-24, -12]`
- `y = 0`
- `z ~ Uniform[2, 13]`

其中 `x` 与目标位置都以各自环境中心为参考。

此外，当前代码还启用了出生高度课程学习：

- 初始最小高度：`8.0`
- 最终最小高度：`2.0`
- 在训练 epoch `2000` 前完成过渡

因此训练初期更偏向较高高度投放，后期再逐步扩展到更低高度范围。

### 9.4 障碍物

当前任务配置中：

`num_obstacles_in_env = 0`

因此，当前默认训练任务可以视作一个无障碍投放任务。

---

## 11. 风场与扰动模型

当前代码中的总风场由两部分组成：

`w_total = w_main + w_local`

### 10.1 主风 Main Wind

主风在每个环境 reset 时采样一次，并在整个 episode 内保持不变：

- 风速范围：`Uniform[1.0, 3.0] m/s`
- 风向：单位向量随机采样
- 当前配置允许 3D 风向，即 `main_wind_horizontal_only = False`

### 10.2 局部扰动 Local Disturbance

当前代码中局部扰动有两种模式：

1. **Dryden 模式**，也是当前默认模式

- 由 `dryden_config.enable_dryden = True` 控制；
- 每个轴在每个 episode 开始时采样 `sigma` 与 `tau`；
- 扰动状态按照一阶随机过程演化。

2. **方向更新式局部阵风模式**

- 仅在关闭 Dryden 后启用；
- 局部风方向会定期重采样，并平滑过渡；
- 局部风强度受每个环境采样得到的 `local_wind_max_speed` 限制。

### 10.3 Critic 看到的风信息

Critic 的特权观测中使用的是：

`wind_world = _compute_gmm_wind_vector(...)`

因此 Critic 看到的是 **当前时刻真实总风场的世界系风速向量**，而不仅仅是主风。

---

## 12. 时间尺度

当前任务的关键时间参数为：

- 仿真物理步长：`dt = 0.01 s`
- 每个环境步平均包含的物理步数：`10`

因此：

- 一个 RL 控制步约等于 `0.1 s` 仿真时间；
- `episode_len_steps = 1000` 对应约 `100 s` 的仿真时长。

这一点在论文里建议明确写出，因为 RL step 与底层物理积分步并不是同一个概念。

---

## 13. 适合论文直接使用的描述

如果你希望在论文正文里直接使用一段较正式的表述，下面这段是和当前代码一致的：

> 本文采用 13 维基础观测，包含机体系下的目标相对位置、机体系线速度、欧拉角 `roll/pitch/yaw`、机体系角速度以及绝对高度。为缓解部分可观测性问题，Actor 端将最近 6 帧基础观测进行堆叠，形成 78 维策略输入。训练阶段进一步采用非对称 Actor-Critic 结构，Critic 在 78 维堆叠观测基础上额外接收 13 维特权物理信息，包括真实世界系风速、当前质量、惯量对角项以及“若当前时刻投放”所对应的预期反冲力与反冲力矩，从而形成 91 维 Critic 输入。部署阶段仅保留 Actor 输入，无需使用特权信息。

---

## 14. 关键维度汇总

- 基础观测维度：`13`
- 帧堆叠长度：`6`
- Actor 输入维度：`78`
- 特权信息维度：`13`
- Critic 输入维度：`91`
- 动作维度：`5`

---

## 15. 与旧版说明的主要差异

如果你手里还有旧截图或旧说明，当前版本和旧版本最重要的差异有：

- 基础观测已经是 `13` 维，不再是 `12` 维；
- `yaw` 已被显式纳入观测；
- Actor 默认使用 `6` 帧堆叠输入；
- Critic 默认使用非对称特权信息；
- 默认目标点固定在环境中心；
- 当前默认风场为主风 + Dryden 湍流；
- 当前默认障碍物数量为 `0`。
