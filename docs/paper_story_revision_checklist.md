# 论文讲故事版二次复查修改清单

本文档基于当前文件重新检查：

`/home/lwulo/下载/论文讲故事版(1).docx`

提取文本缓存：

`/tmp/paper_story_text_current_v2.txt`

当前总体判断：这版已经修掉了很多关键问题，包括 `18维观测`、实验二表格错位、实验三中风姿态表、极端事件表统计口径、总结和展望等。但仍有一些必须继续改的地方，主要集中在摘要、奖励函数公式、实验三风场描述、贡献点重复、图注措辞和参考文献。

## 1. 已经基本修好的部分

| 检查项 | 当前状态 | 说明 |
|---|---|---|
| 观测维度 | 已修正 | 正文已写 `18维`，并说明 `15维基础观测 + 3维投放预测特征` |
| 投放预测特征 | 基本修正 | 已说明不输入真实风速，三个特征的含义基本正确 |
| 物理建模错字 | 已修正 | 上一版的“头发干瞬间”已删除 |
| 实验二表格 | 已修正 | `Proposed / PPO-GRU / MPC` 数据行已对应正确 |
| 实验三中风姿态表 | 已修正 | 表5-8 已是正确姿态/冲击数据 |
| 网络结构错字 | 已修正 | “三两层”已改为“两层全连接网络” |
| 输出层描述 | 已修正 | 已删除“依据原始报告表述” |
| 超参数表 | 基本修正 | 已改为 `3e-5 / 0.15 / 0.003` 等当前配置 |
| 实验一旧结论 | 已修正 | 已删除“显著优于PPO”的不严谨表述 |
| 表5-13 | 基本修正 | 已改为当前正文展示的 6 条件统计口径，数值正确 |
| 表5-14 | 基本修正 | 已改为当前正文展示的 6 条件统计口径，分析段也基本匹配 |
| 总结和展望 | 已补充 | 已有基本总结和后续方向 |

## 2. 仍需优先修改的问题

### 2.1 摘要仍缺少实验结果

位置：

- 摘要

当前问题：

当前摘要只写到方法和奖励函数，没有给任何实验定量结果。摘要应包含方法和主要结果，否则不完整。

建议在摘要最后补充：

```text
基于Isaac Gym平台开展多组对比实验，将本文方法与标准PPO、GRU-only PPO以及MPC方法进行比较。实验结果表明，本文方法在固定高度、随机障碍、多高度、多风速以及综合复杂场景下均表现出较好的落点精度和整体任务成功率。在当前论文展示的实验条件合并统计中，本文方法的平均落点误差为2.777 m，P90误差为8.792 m，Overall@3m为78.79%，均优于标准PPO和GRU-only PPO；相较于MPC，本文方法在保持较高投放率的同时显著提升整体命中率。需要指出的是，MPC在成功投放样本中的释放姿态和冲击指标最优，但其投放率较低，导致整体任务完成能力受限。
```

### 2.2 贡献点第二条句子重复

位置：

- 绪论 > 主要贡献

当前文本存在重复：

```text
提出一种面向子母式无人机的智能投放决策框架，提出一种面向子母式无人机投放任务的PPO-MLP-GRU智能决策框架。
```

建议改成一句：

```text
提出一种面向子母式无人机投放任务的 PPO-MLP-GRU 智能决策框架。该框架首先利用MLP提取当前观测的瞬时特征，再通过GRU对历史状态序列进行递推建模，实现投放时机、落点精度与释放姿态稳定性的联合优化。
```

### 2.3 贡献点第一条建议收敛表述

位置：

- 绪论 > 主要贡献

当前写法：

```text
构建融合偏心挂点反冲力矩与等效反冲力的子机投放物理模型
```

建议改为更完整、更贴合当前代码：

```text
构建事件驱动的投放瞬态等效动力学模型，刻画子机释放引起的母机质量、质心和惯量阶跃变化，以及相对于更新后质心的等效反冲力与偏心反冲力矩。
```

### 2.4 奖励函数公式仍有旧逻辑

位置：

- 奖励函数的设计
- 冲击惩罚
- 投放表现奖励公式
- 总奖励公式前的说明

#### 2.4.1 冲击惩罚文字仍把 `alpha` 说成姿态角权重

当前文本：

```text
其中，α姿态角变化量的权重系数，β角速度变化量的权重系数；λimp表示冲击惩罚的强度系数；Δv表示投放前后母机线速度变化量；Δω表表示投放前后机体系角速度变化量。
```

问题：

当前公式已经改成 `Δv`，但解释仍写“姿态角变化量”；并且有错字 `Δω表表示`。

建议改为：

```text
其中，α为线速度突变权重，β为角速度突变权重，λimp为冲击惩罚强度系数；Δv表示投放前后母机线速度变化量，Δω表示投放前后机体系角速度变化量。该项用于抑制释放瞬间由弹射反冲和偏心力矩引起的速度与角速度突变。
```

#### 2.4.2 冲击惩罚公式建议加括号

当前文本：

```text
Rimp=−λimp⋅α∥Δv∥2+β∥Δω∥2
```

这个表达容易被理解为：

```text
(-λimp * α * ||Δv||) + β * ||Δω||
```

建议改为：

```text
Rimp = -λimp ( α ||Δv||2 + β ||Δω||2 )
```

### 2.5 `Rdrop` 公式仍然保留旧的 `if exy <= douter`

位置：

- 投放表现奖励公式

当前文本：

```text
Rdrop=wscoreS+Rimp+Ratt if exy≤douter
```

问题：

当前代码不是“只在误差小于阈值才有投放奖励”。当前逻辑是连续指数得分，超过外层区域或撞障碍后才覆盖为 `-outside_region_penalty`。

建议改为：

```text
Rdrop = wscore S(e_xy) + Rimp + Ratt,    e_xy <= d_outer 且未碰撞障碍物
Rdrop = -P_out,                          e_xy > d_outer 或落点碰撞障碍物
```

并在文字中说明：

```text
其中 d_outer 为最外层允许误差边界，当前设置为8.0 m；P_out 为无效投放惩罚，当前设置为20.0。
```

### 2.6 风险感知逻辑仍缺少 5m 合理不投放机制

位置：

- 风险感知逻辑
- 超时状态的后验影子评价

当前问题：

你现在只写了“超时后执行虚拟投放预测”，但没有说明如何根据预测误差奖励或惩罚 no-drop。

建议在“超时状态的后验影子评价”后加入：

```text
对于超时且未投放的episode，系统在终止时刻执行一次虚拟投放预测。若预测落点误差大于5m，则说明当前状态下投放风险较高，不投放被视为合理保守决策，并给予小额奖励；若预测落点误差不超过5m，则认为策略错失了可行投放窗口并施加惩罚。若episode因碰撞结束且始终未投放，则视为失败并施加较大惩罚。
```

当前参数：

| 项 | 值 |
|---|---:|
| 合理不投放阈值 | `5.0 m` |
| 合理不投放奖励 | `+2.0` |
| 错失投放惩罚 | `-8.0` |
| 撞毁未投放惩罚 | `-20.0` |

### 2.7 总奖励公式前的解释仍说 `Rdrop` 和 `Ratt` 分开

位置：

- 总奖励公式前一段

当前文本大意：

```text
整合 Rprog、Ralt、Rdrop 与 Ratt
```

但你最终公式已经写：

```text
Rt = Rprog + Rdrop + Rno-drop + Ralt
```

建议把公式前说明改为：

```text
其中，Rprogress 为投放前方向进展奖励，Rdrop 为执行投放时的综合投放奖励，内部包含投放精度得分、冲击惩罚、姿态稳定奖励以及无效投放惩罚；Ralt 为高度软约束项；Rno-drop 为episode结束但未投放时的后验风险感知奖励或惩罚。
```

### 2.8 训练参数表建议补全关键参数

位置：

- 训练参数设置表

当前表格已有核心 PPO 参数，但缺少：

| 参数 | 当前值 |
|---|---:|
| `seq_length` | 16 |
| `num_envs` | 1024 |
| `max_epochs` | 10000 |
| `normalize_input` | True |
| `normalize_value` | True |

建议补入表格，尤其是 `seq_length=16`，因为你的方法强调 GRU 时序建模。

### 2.9 实验三仍写“阵风序列”，应改为“主风速度等级”

位置：

- 实验三开头说明段

当前文本：

```text
通过设置不同强度的阵风序列以及随机起始高度...
```

问题：

论文实验三对应的是：

```text
result_exp4_multiH_multiW_noObs/exp4_multiH_multiW_noObs
```

该实验设置为：

```text
主风速度：1 / 2 / 3 m/s
随机高度：4-13m
无障碍
Dryden = False
local wind = 0
```

所以不能写“阵风序列”。

建议替换为：

```text
本实验用于验证策略在随机高度与不同主风速度等级耦合条件下的投放时机泛化能力。实验设置1 m/s、2 m/s和3 m/s三个主风速度等级，并将初始高度随机化为4-13 m，同时关闭障碍物、Dryden湍流和局部GMM风扰，以隔离高度变化与主风速度变化对投放性能的影响。
```

### 2.10 实验四建议说明 Dryden 和局部风打开

位置：

- 实验四开头

当前实验四标题：

```text
有障碍，多高度，多风强的综合复杂场景
```

建议补充一句：

```text
综合复杂场景进一步叠加随机高度、多主风速度、随机障碍物、局部GMM风扰以及Dryden湍流扰动，用于验证策略在更复杂扰动条件下的综合鲁棒性。
```

注意：实验三不能写 Dryden；实验四可以写 Dryden。

### 2.11 图5-16措辞仍不自然

位置：

- 图5-16

当前文本：

```text
图5-16 实验四投放时母机精度CDF图
```

建议改为：

```text
图5-16 实验四投放精度CDF图
```

图5-17和图5-18当前基本可以：

```text
图5-17 实验四投放冲击CDF图
图5-18 实验四投放时母机姿态数据图
```

### 2.12 综合极端指标定义中缺少 theta 公式

位置：

- 综合极端事件分析第一段

当前文本：

```text
姿态稳定性方面，定义合成释放姿态角，并统计其P90、P95、CVaR90以及theta > 30deg的比例。
```

问题：

没有写合成姿态角公式。

建议改为：

```text
姿态稳定性方面，定义合成释放姿态角 theta = sqrt(roll^2 + pitch^2)，并统计其P90、P95、CVaR90以及theta > 30deg的比例。
```

### 2.13 `PPO(baseline)` 命名仍未统一

位置：

- 所有实验表格

当前 Word 仍大量使用：

```text
PPO(baseline)
```

建议统一为：

```text
PPO
```

如果担心读者不清楚，第一次出现时写：

```text
PPO，即不含循环结构的标准PPO基线方法。
```

后续表格统一用 `PPO`。

### 2.14 实验二标题建议改为“随机障碍”

位置：

- 实验二标题

当前文本：

```text
实验二： 固定高度、固定风速、有障碍环境
```

建议改为：

```text
实验二：固定高度、固定风速、随机障碍环境
```

原因：表5-3和表5-4使用的是 `random_obstacle` 数据，不是固定障碍，也没有展示 no-obstacle 对照。

### 2.15 实验二缺少分析段

位置：

- 表5-4 后，实验三之前

当前问题：

实验二只有表，没有文字分析。建议补一段，否则实验二显得突兀。

建议加入：

```text
在固定高度、固定风速和随机障碍条件下，Proposed取得最低中位误差和最低P90误差，R@3m达到87.15%，说明其在存在障碍扰动时仍能保持较稳定的投放时机判断。PPO-GRU的投放率略高，但P90误差明显大于Proposed，表明仅使用循环记忆仍存在尾部偏差风险。MPC在姿态和冲击指标上最优，但投放率仅为51.50%，导致整体任务完成能力受限。
```

### 2.16 表5-13和表5-14数值已正确，但排序建议统一

位置：

- 表5-13
- 表5-14

当前表5-13顺序：

```text
PPO / PPO-GRU / MPC / Proposed
```

当前表5-14顺序：

```text
PPO / Proposed / PPO-GRU / MPC
```

建议所有表统一为：

```text
PPO
Proposed
PPO-GRU
MPC
```

这样读者更容易横向比较，也和你论文中“本文方法在第二行突出显示”更一致。

### 2.17 表5-13和表5-14对应分析段建议版本

位置：

- 表5-13后分析段
- 表5-14后分析段

当前判断：

当前 Word 中这两段已经比上一版合理，特别是表5-14后已经说明 Proposed 不是姿态角最小的方法。但为了让表格替换后的分析完全对应，建议按下面版本统一替换。

表5-13后建议段落：

```text
从综合投放精度尾部风险看，Proposed的平均误差、P90误差和Overall@3m均优于学习类基线。具体而言，Proposed的平均误差为2.777 m，P90误差为8.792 m，Overall@3m为78.79%；相比之下，PPO的P90误差达到20.622 m，说明其在复杂条件下存在明显长尾偏差；PPO-GRU的P90误差为11.675 m，优于PPO但仍高于Proposed，表明仅依赖GRU递推记忆不足以完全替代MLP前端对当前瞬时状态特征的提取。MPC在已投放样本中的R@3m为66.89%，但由于Drop Rate仅为52.28%，其Overall@3m下降至34.97%，说明MPC通过保守释放策略规避了大量困难样本，整体任务完成能力受限。
```

表5-14后建议段落：

```text
从释放姿态极端事件看，MPC在成功释放样本中的姿态分布最集中，其P95 theta仅为5.28 deg，theta>30deg比例为0.22%，说明模型预测控制在满足释放条件时具有最稳定的释放姿态。相比之下，学习方法的姿态尾部风险明显更高。Proposed的P90 theta为35.01 deg，略低于PPO-GRU的36.51 deg；theta>30deg比例为12.61%，也略低于PPO-GRU的12.95%，说明本文方法在一般尾部姿态风险上略优于GRU-only基线。但Proposed的P95 theta为69.34 deg，略高于PPO-GRU的68.00 deg，说明其在最极端少数样本上仍存在较大姿态释放风险。因此，本文方法的主要优势应表述为降低投放精度尾部风险和提高整体命中率，而不是取得最小释放姿态角；姿态尾部安全仍是后续需要进一步优化的方向。
```

### 2.18 总结和展望建议加入定量结论

位置：

- 总结和展望

当前总结已有，但没有定量数据。建议补一句：

```text
在当前论文展示条件的合并统计中，Proposed的平均误差为2.777 m，P90误差为8.792 m，Overall@3m为78.79%，均优于PPO和PPO-GRU。
```

也可以补一句局限：

```text
但姿态尾部指标显示，Proposed的P95 theta仍达到69.34 deg，说明极端姿态释放风险仍需进一步抑制。
```

### 2.19 参考文献仍存在明显格式问题

位置：

- 参考文献

当前问题：

1. `[1]` 是 `IEEE Xplore Full-Text PDF`，没有作者、题名、期刊等完整信息。
2. `[14]` 仍是 `SHI J...`，看起来可能是旧重复文献或未重新核对。
3. arXiv、网页、期刊格式不统一。
4. `[8]`、`[9]` 为 2026 文献，建议你确认确实能被检索到。

建议：

```text
将[1]补全为标准参考文献格式；删除或替换重复/无效文献；统一所有参考文献为学校要求格式。
```

## 3. 建议但不一定必须改的内容

### 3.1 公式排版需要人工检查

提取文本中公式仍显示为：

```text
Mdyn=j∈Smj
pCoMnew=1Mdynj∈Smjpj
Itotal=j∈SRjIbody,jRjT+mj[dj×]2
```

这可能只是 Word 公式被文本提取后丢失求和符号，也可能是公式本身排版有问题。请在 Word 里人工检查。

建议公式表达为：

```text
M_dyn = sum_{j in S} m_j
p_CoM_new = (1 / M_dyn) sum_{j in S} m_j p_j
I_total = sum_{j in S} (R_j I_body,j R_j^T + m_j (||d_j||^2 I - d_j d_j^T))
```

### 3.2 摘要关键词可以保留 Dryden，但正文要区分训练/实验

当前关键词中有：

```text
湍流模型(Dryden)
```

可以保留，因为训练默认环境和综合复杂场景确实涉及 Dryden。但实验三不能写 Dryden，实验三是关闭 Dryden 后的多高度、多主风速度实验。

### 3.3 “高保真”表述建议收敛

正文仍有：

```text
高保真动力学反馈
高保真物理环境
```

你的模型是事件驱动等效模型，不是完整连续多体分离仿真。更稳妥的写法是：

```text
物理信息增强的仿真环境
```

或：

```text
包含投放瞬态等效扰动的仿真环境
```

## 4. 当前最小修改顺序

如果时间有限，按这个顺序改：

| 优先级 | 修改项 |
|---|---|
| 1 | 摘要补充实验定量结果 |
| 2 | 修正贡献点第二条重复句 |
| 3 | 修正奖励函数：`Rimp`解释、`Rdrop`分段公式、5m合理不投放逻辑 |
| 4 | 实验三开头删除“阵风序列”，改为主风速度等级 |
| 5 | 实验四补充 Dryden + 局部GMM风扰说明 |
| 6 | 图5-16改为“实验四投放精度CDF图” |
| 7 | 综合极端指标定义补上 `theta = sqrt(roll^2 + pitch^2)` |
| 8 | 实验二补一段分析文字 |
| 9 | 表5-13/表5-14后分析段按当前数值重新表述 |
| 10 | 统一表格方法名顺序和 `PPO(baseline)` 命名 |
| 11 | 总结和展望补定量结论 |
| 12 | 参考文献格式统一，尤其修正 `[1]` |

## 5. 当前表5-13/5-14统计口径说明

你当前表5-13和表5-14采用的是“当前正文展示的 6 个条件”合并统计，口径是合理的。

这 6 个条件是：

1. 实验一：固定高度、固定风速、无障碍
2. 实验二：固定高度、固定风速、随机障碍
3. 实验三：低风速、多高度、无障碍
4. 实验三：中风速、多高度、无障碍
5. 实验三：高风速、多高度、无障碍
6. 实验四：综合复杂场景

对应总样本：

```text
6 条件 × 2000 case = 12000 case / method
```

因此表5-13/5-14现在不需要再用原来的 16000 case 综合表。

## 6. 主要依据文件

| 用途 | 文件 |
|---|---|
| 当前 Word 提取文本 | `/tmp/paper_story_text_current_v2.txt` |
| 论文修改清单 | `docs/paper_story_revision_checklist.md` |
| 极端事件加入指南 | `docs/extreme_event_analysis_insertion_guide.md` |
| 当前实验结果分析 | `docs/experiment_results_analysis_result_with_saved_traces.md` |
| 网络与观测说明 | `docs/navigation_task_gmm_noise_network_spec.md` |
| 奖励函数说明 | `docs/drop_reward_function_spec.md` |
| 投放物理模型说明 | `docs/drop_model_formula_spec.md` |
| Proposed 训练配置 | `aerial_gym/rl_training/rl_games/ppo_aerial_quad_navigation_mlp_rnn_lite.yaml` |
| 环境观测和奖励代码 | `aerial_gym/task/navigation_task_gmm_noise/navigation_task_gmm_noise.py` |
| 对比实验设置 | `aerial_gym/scripts/eval_drop_paper_comparison.py` |
